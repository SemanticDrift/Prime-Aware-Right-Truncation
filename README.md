# Prime-Aware-Right-Truncation
A deterministic descent operator that navigates the prime landscape using exactly one binary choice per step. Every prime reaches a terminal state in finite steps. Right-truncation is not a curiosity. It is a structural probe of prime stability. Run it and see where it lands.
